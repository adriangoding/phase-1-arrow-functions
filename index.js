const divide = (parameter, parameter2) => parameter / parameter2;
divide(2000,100);

const square = (parameter) => parameter * parameter;
square(5);

const add = (a, b) => {
    return a + b;
  };
  add(3, 4);